package com.poketeambuilder.repositories;

import com.poketeambuilder.entities.Type;

/** CRUD + specification queries for {@link Type} reference data. */
public interface TypeRepository extends BaseRepository<Type, Integer> {
    
}

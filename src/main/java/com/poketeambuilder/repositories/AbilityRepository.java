package com.poketeambuilder.repositories;

import com.poketeambuilder.entities.Ability;

/** CRUD + specification queries for {@link Ability} reference data. */
public interface AbilityRepository extends BaseRepository<Ability, Integer> {
    
}

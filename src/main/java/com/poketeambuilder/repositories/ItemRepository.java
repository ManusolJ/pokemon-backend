package com.poketeambuilder.repositories;

import com.poketeambuilder.entities.Item;

/** CRUD + specification queries for {@link Item} reference data. */
public interface ItemRepository extends BaseRepository<Item, Integer> {
    
}

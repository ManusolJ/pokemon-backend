package com.poketeambuilder.repositories;

import com.poketeambuilder.entities.Nature;

/** CRUD + specification queries for {@link Nature} reference data. */
public interface NatureRepository extends BaseRepository<Nature, Integer> {
    
}
